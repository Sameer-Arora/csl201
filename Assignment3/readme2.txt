The code is complied by the command g++ main.cpp as main.cpp is its main file .
The header files included are mavl.hpp.
The input files are entries.txt.
The print..etc are utility functions for the users nesccessary comments are added. 
In case the keys are not there in the tree junk values are returned with proper error messages.